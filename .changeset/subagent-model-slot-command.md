---
"oh-my-kimi-code": patch
---

Extend `/subagent-model` with named binding slot management: `set slot <name>` / `clear slot <name>` subcommands and a two-section `list` (types and slots), backed by new `getSubagentSlotBindings` / `setSubagentSlotBinding` session RPCs.
